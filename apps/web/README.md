# Web App Target

Destino de migracion para:

- web publica multidioma
- portal autenticado de agentes/clientes
- APIs publicas y portal

Mientras la migracion siga siendo compatible, el codigo fuente principal sigue en `src/pages/[lang]` y `src/pages/api/v1/portal`.

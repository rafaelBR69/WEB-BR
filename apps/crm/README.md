# CRM App Target

Destino de migracion para:

- backoffice CRM
- APIs privadas `/api/v1/crm/*`
- layout, assets y auth interna del CRM

Mientras la migracion siga siendo compatible, el codigo fuente principal sigue en `src/pages/crm` y `src/pages/api/v1/crm`.
